name: CI

on:
  push:
    branches:
      - main && dev
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: check code
        run: echo "Good morning Francis"
        shell: bash